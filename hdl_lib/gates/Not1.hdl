entity Not1 is
  port(
    a : in bit;
    y : out bit
  );
end entity;

architecture rtl of Not1 is
  component Nand2
    port(
      a : in bit;
      b : in bit;
      y : out bit
    );
  end component;
begin
  u_nand: Nand2 port map (a => a, b => a, y => y);
end architecture;
